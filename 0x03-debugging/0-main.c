#include "main.h"

/**
 * main - entry into the program
 *
 * Description: test the function positive_or_negative
 * Return: return 0 when successful
 */

int main(void)
{
	int zero = 0;

	positive_or_negative(zero);
	return (0);
}
