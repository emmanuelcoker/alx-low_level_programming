#include "main.h"

/**
 * _islower - check is char is lower case
 * @c: character to check
 *
 * Description: check lowercase character
 * Return: return 1 or 0
 */

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
