#include <stdio.h>
#include "main.h"

/**
 * _isdigit - checks if value is a digig
 *
 * @c: ascii code for the digit
 * Description: a program to check if adigit is between 0 to 9
 * Return: return 1 if digit else return 0
 */

int _isdigit(int c)
{
	if(c >= '0' && c <= '9')
		return (1)
	return (0);
}
