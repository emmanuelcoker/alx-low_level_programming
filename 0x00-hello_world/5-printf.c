#include<stdio.h>
/**
 *main - Entry point
 *
 *Return: Always 0
 */

int main(void)
{
	char *message = "with proper grammar, but the outcome is a piece of art,\n";

	printf("%s", message);
	return (0);
}
